packages:
  - 'packages/*'
  - 'apps/*'
# esbuild isporučuje predkompilirani binary preko install skripte; Vite bez
# njega ne radi. Ostale skripte ostaju blokirane.
allowBuilds:
  esbuild: true
  # Skripta dohvaća wasm jezgru u vlastiti `dist`. Bez nje OCR nema što učitati.
  tesseract.js: true
