{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window. File system access does NOT go through the fs plugin but through our own commands in ul-core, which enforce a sandbox rooted at the workspace.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
